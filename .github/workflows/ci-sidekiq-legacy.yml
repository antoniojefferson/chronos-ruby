name: Legacy Sidekiq CI

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read

jobs:
  example:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        include:
          - image: ruby:2.2.10
            example: examples/sidekiq-4
          - image: ruby:2.5.9
            example: examples/sidekiq-5
    name: ${{ matrix.image }} / ${{ matrix.example }}
    steps:
      - uses: actions/checkout@v4
        with:
          persist-credentials: false
      - name: Run Sidekiq compatibility smoke
        run: |
          docker run --rm \
            --volume "$PWD:/app" \
            --workdir /app \
            --env EXAMPLE_PATH="${{ matrix.example }}" \
            "${{ matrix.image }}" \
            sh script/ci_sidekiq_legacy
